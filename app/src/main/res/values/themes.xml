<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.WhatsAppGemini" parent="android:Theme.Material.Light.NoActionBar" />

    <style name="Theme.App.Starting" parent="Theme.SplashScreen">
        <!-- Set the splash screen background, animated icon, and animation
        duration. -->
        <item name="windowSplashScreenBackground">@color/splash_background</item>

        <!-- Use windowSplashScreenAnimatedIcon to add a drawable or an animated
             drawable. One of these is required. -->
        <item name="windowSplashScreenAnimatedIcon">@drawable/whatsapp_splash</item>
        <!-- Required for animated icons. -->
        <item name="postSplashScreenTheme">@style/Theme.WhatsAppGemini</item>
    </style>
</resources>